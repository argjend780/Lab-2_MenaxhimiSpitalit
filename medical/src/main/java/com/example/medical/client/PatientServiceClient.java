package com.example.medical.client;

/*import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

import com.example.medical.dto.PacientiDTO;

@FeignClient(name = "meaxhimi-spitalit-app", url = "${feign.address.meaxhimi-spitalit-app}")
public interface PatientServiceClient {

    @GetMapping(path = "/api/pacient/o/{qytetiId}/{spitaliId}/{repartiId}/{pacinetId}")
    PacientiDTO getPacientById(
        @PathVariable Long qytetiId,
        @PathVariable Long spitaliId,
        @PathVariable Long repartiId,
        @PathVariable Long pacinetId
    );
}*/